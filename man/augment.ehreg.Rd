% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{augment.ehreg}
\alias{augment.ehreg}
\title{augment method for ehreg (Extended Hazard) models}
\usage{
\method{augment}{ehreg}(x, data = NULL, newdata = NULL, ...)
}
\arguments{
\item{x}{a fitted model object of class \code{"ehreg"}.}

\item{data}{a data frame to augment. Defaults to the model frame used
during fitting (covariates only, response excluded). Ignored when
\code{newdata} is supplied.}

\item{newdata}{an optional data frame of new observations. When provided,
both linear predictors and their standard errors are computed from these
data. Because observed outcomes are not available, \code{.time}, \code{.event}, and
all residual columns are set to \code{NA}.}

\item{...}{further arguments passed to or from other methods.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per observation. The first two
columns are:
\describe{
\item{\code{.time}}{observed failure/censoring time;}
\item{\code{.event}}{event indicator (1 = event, 0 = censored).}
}
Followed by the original covariate columns and:
\describe{
\item{\code{.fitted.AF}}{accelerated failure linear predictor
\eqn{X\hat{\beta}_{\text{AF}}};}
\item{\code{.se.fit.AF}}{standard error of the AF linear predictor;}
\item{\code{.fitted.RH}}{relative hazard linear predictor
\eqn{X\hat{\beta}_{\text{RH}}};}
\item{\code{.se.fit.RH}}{standard error of the RH linear predictor;}
\item{\code{.resid.coxsnell}}{Cox-Snell residuals, or \code{NA} when \code{newdata}
is supplied;}
\item{\code{.resid.martingale}}{martingale residuals, or \code{NA} when \code{newdata}
is supplied;}
\item{\code{.resid.deviance}}{deviance residuals, or \code{NA} when \code{newdata}
is supplied.}
}
}
\description{
augment method for ehreg (Extended Hazard) models
}
\examples{
library(survival)
library(survstan)
fit <- ehreg(Surv(futime, fustat) ~ age + rx, data = ovarian, dist = "weibull")

# Augment training data
augment(fit)

# Predictions on new observations
augment(fit, newdata = data.frame(age = c(50, 60, 70), rx = c(1, 2, 1)))
}
